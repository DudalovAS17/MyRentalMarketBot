"""
    except (ValueError, PermissionError) as e:
        # Ошибка бизнес-логики: не тот статус / не владелец / не найдена сделка и т.п.
        await callback.answer(str(e), show_alert=True) # (f"❌ {e}")
"""

""" Ошибки (у каждого — своё место обработки):

2. <><><><><><><><><><><><><><><><><><> Бизнес-логика <><><><><><><><><><><><><><><><><><><><><><><><><><><>
(не тот статус / не тот пользователь / сделка не найдена / двойной клик / устаревшая кнопка)
    - это то, что сервис должен уметь сообщать контролируемо
    - ловим в хендлере [action-кнопки (confirm/reject/cancel)] и показываем пользователю
if not ok - “бизнес-отказ”: система работает, но по правилам действие выполнить нельзя.

Причины:
    ^ статус уже изменился (двойной клик, другой участник успел изменить)
    ^ кнопка устарела (старое сообщение)
    ^ пользователь не тот (не владелец)
    ^ переход статусов запрещён (REQUESTED → CONFIRMED только владельцем)

Идеальный ответ: (UX-реакция)
    - показать понятное короткое сообщение (часто show_alert=True, чтобы точно заметил)
    - и перерисовать UI, чтобы пользователь увидел актуальный статус/кнопки

3. <><><><><><><><><><><><><><><><><><> UI-рендеринг (редактирование сообщения) <><><><><><><><><><><><><><><><><><> 
    - это TelegramBadRequest
    - ловим в render-функции, потому что это “транспорт/интерфейс”

TelegramBadRequest — это “нет текста”, “message is not modified”, “can't edit message”, 
“message to edit not found”

Рекомендация: не ловить “вслепую” и всегда слать новое сообщение.
    - если “message is not modified” → просто проигнорировать
    - если “there is no text in the message to edit” / “message can't be edited” → отправить новое
    
4. <><><><><><><><><><><><><><><><><><> Неожиданная ошибка <><><><><><><><><><><><><><><><><><><><><><><><><><><><>
    - это Exception
    - ловим в хендлере, логируем, показываем “Попробуйте позже”

Причины:
    ^ БД недоступна / таймаут / locked (SQLite)
    ^ ошибка запроса
    ^ баг в коде сервиса
    ^ неожиданное исключение из зависимостей

Идеальный ответ: (это не вина пользователя и не бизнес-ситуация, поэтому)
    - логируем logger.exception(...) (чтобы видеть traceback)
    - показываем пользователю нейтральное сообщение: “Ошибка. Попробуйте позже.”
    - и выходим (return), потому что продолжать опасно: состояние неизвестно (статус мог обновиться, мог не обновиться).
    - позже можно сузить Exception до SQLAlchemyError/IntegrityError, но общий принцип остаётся тот же.
    


==========================================================================================
Почему перерисовка — и при успехе, и при fail:

Inline-кнопки живут в сообщении. Пользователь ожидает, что после нажатия: !статус обновится или !кнопки изменятся
Если не перерисовать, пользователь видит старые кнопки и снова нажимает → спам, путаница.

Поэтому:
    - после успеха перерисовка обязательна
    - после бизнес-fail перерисовка очень полезна (покажет актуальный статус)
    - после инфраструктурной ошибки перерисовку часто НЕ делают, потому что не ясно, что произошло (и можно создать 
    дополнительные ошибки)

==========================================================================================
Сводка по 3 веткам (самая важная часть):

Некорректная кнопка (парсинг не удался)
→ показать alert и выйти (нет смысла продолжать)

Инфраструктурная ошибка (сервис упал)
→ лог + alert “попробуйте позже” + выйти (неизвестно, что реально произошло)

Бизнес-отказ (ok == False)
→ alert “не удалось” + перерисовать детали (пользователь должен увидеть актуальное)

Успех (ok == True)
→ короткий toast “Подтверждено” + перерисовать детали

==========================================================================================

Если ты хочешь показать пользователю конкретный текст (успех/ошибка) — вызывай answer(text=..., show_alert=...).
Если ты не хочешь ничего показывать — await callback.answer() один раз в начале.

Стандарт:
    action-хендлер: await callback.answer() в конце (или один раз в нужной ветке с текстом)
    show-details-хендлер: await callback.answer() сразу, чтобы убрать “часики”.
"""