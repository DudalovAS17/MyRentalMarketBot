# Repository

✅ не логирует успех (это шум). Логировать успех = в сервисе на бизнес-событии.

✅ не ловит Exception

✅ ловит только если хотим “нормализовать” конкретную DB ошибку (например IntegrityError → None/0) 
и логируем. Иначе пусть летит наверх.

✅ (временно) делает rollback при ошибке commit - см. ниже

--- 
### **ROLLBACK:**
Почему желательно убрать rollback?  Потому что: 
- commit при ошибке выбросит исключение        
- сессия после ошибки обычно требует rollback, да
но в реальных проектах rollback делают:        
  - либо в unit-of-work / transaction middleware
  - либо в repo только на конкретных нормализуемых ошибках
        
но пока так:

**Правило №3 (Core)**:

Любой write-метод репозитория (create/update/delete) обязан:
- commit()
- при исключении: rollback() и пробросить исключение дальше

**Правило №4 (Core):**

Rollback делаем только вокруг commit, а не “вокруг всего метода”. 


---